$OpenBSD$

Index: openvdb/python/CMakeLists.txt
--- openvdb/python/CMakeLists.txt.orig
+++ openvdb/python/CMakeLists.txt
@@ -3,7 +3,7 @@ PROJECT ( PyOpenVDB )
 
 FIND_PACKAGE ( PythonInterp REQUIRED )
 FIND_PACKAGE ( PythonLibs REQUIRED )
-FIND_PACKAGE ( Boost ${MINIMUM_BOOST_VERSION} REQUIRED COMPONENTS python${PYTHON_VERSION_MAJOR}.${PYTHON_VERSION_MINOR} )
+FIND_PACKAGE ( Boost ${MINIMUM_BOOST_VERSION} REQUIRED COMPONENTS python${PYTHON_VERSION_MAJOR} )
 
 
 IF ( NOT OPENVDB_BUILD_CORE )
