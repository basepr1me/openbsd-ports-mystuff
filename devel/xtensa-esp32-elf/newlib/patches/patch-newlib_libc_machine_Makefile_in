$OpenBSD$

Index: newlib/libc/machine/Makefile.in
--- newlib/libc/machine/Makefile.in.orig
+++ newlib/libc/machine/Makefile.in
@@ -577,6 +577,11 @@ uninstall-am:
 $(machine_dir)/lib.a:
 
 doc:
+	for d in $(SUBDIRS); do \
+	  if test "$$d" != "."; then \
+	    (cd $$d && $(MAKE) TARGETDOC=$(TARGETDOC) doc) || exit 1; \
+	  fi; \
+	done
 
 # Tell versions [3.59,3.63) of GNU make to not export all variables.
 # Otherwise a system limit (for SysV at least) may be exceeded.
