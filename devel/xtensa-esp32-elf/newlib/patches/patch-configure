$OpenBSD$

Index: configure
--- configure.orig
+++ configure
@@ -2769,7 +2769,6 @@ libgcj="target-libffi \
 # Note that libiberty is not a target library.
 target_libraries="target-libgcc \
 		target-libbacktrace \
-		target-libgloss \
 		target-newlib \
 		target-libgomp \
 		target-libatomic \
@@ -2786,6 +2785,9 @@ target_libraries="target-libgcc \
 		target-libobjc \
 		target-libada \
 		target-libgo"
+#TENSILICA-LOCAL - We have our own libgloss, so skip newlib's
+#		target-libgloss
+#TENSILICA-LOCAL
 
 # these tools are built using the target libraries, and are intended to
 # run only in the target environment
