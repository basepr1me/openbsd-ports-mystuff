$OpenBSD$

Obviously manually set wxWidgets to stable

Index: src/CMakeLists.txt
--- src/CMakeLists.txt.orig
+++ src/CMakeLists.txt
@@ -50,7 +50,7 @@ if (SLIC3R_GUI)
             endif ()
         endif ()
     else ()
-        find_package(wxWidgets 3.1 REQUIRED COMPONENTS html adv gl core base)
+        find_package(wxWidgets 3.0 REQUIRED COMPONENTS html adv gl core base)
     endif ()
 
     if(UNIX)
