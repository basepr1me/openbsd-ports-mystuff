$OpenBSD$

Index: pcb_calculator/CMakeLists.txt
--- pcb_calculator/CMakeLists.txt.orig
+++ pcb_calculator/CMakeLists.txt
@@ -79,6 +79,8 @@ target_link_libraries( pcb_calculator
     #singletop  # replaces common, giving us restrictive control and link warnings.
     # There's way too much crap coming in from common yet.
     common
+    stdc++
+    c
     ${wxWidgets_LIBRARIES}
     )
 
@@ -99,6 +101,8 @@ set_target_properties( pcb_calculator_kiface PROPERTIE
     )
 target_link_libraries( pcb_calculator_kiface
     common
+    stdc++
+    c
     markdown_lib
     ${wxWidgets_LIBRARIES}
     )
