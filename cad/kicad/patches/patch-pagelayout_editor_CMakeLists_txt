$OpenBSD$

Index: pagelayout_editor/CMakeLists.txt
--- pagelayout_editor/CMakeLists.txt.orig
+++ pagelayout_editor/CMakeLists.txt
@@ -87,6 +87,8 @@ target_link_libraries( pl_editor
     #singletop  # replaces common, giving us restrictive control and link warnings.
     # There's way too much crap coming in from common yet.
     gal
+    stdc++
+    c
     common
     ${wxWidgets_LIBRARIES}
     )
@@ -105,6 +107,8 @@ add_library( pl_editor_kiface MODULE
     )
 target_link_libraries( pl_editor_kiface
     gal
+    stdc++
+    c
     common
     ${wxWidgets_LIBRARIES}
     ${GDI_PLUS_LIBRARIES}
