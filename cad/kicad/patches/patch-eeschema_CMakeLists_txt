$OpenBSD$

Index: eeschema/CMakeLists.txt
--- eeschema/CMakeLists.txt.orig
+++ eeschema/CMakeLists.txt
@@ -369,6 +369,8 @@ target_link_libraries( eeschema
     # There's way too much crap coming in from common yet.
     gal
     common
+    stdc++
+    c
     ${wxWidgets_LIBRARIES}
     )
 
@@ -400,6 +402,8 @@ target_include_directories( eeschema_kiface PUBLIC
 
 target_link_libraries( eeschema_kiface
     common
+    stdc++
+    c
     sexpr
     markdown_lib
     scripting
@@ -409,6 +413,8 @@ target_link_libraries( eeschema_kiface
 
 if( KICAD_SPICE )
     target_link_libraries( eeschema_kiface
+        stdc++
+        c
         ${NGSPICE_LIBRARY}
         )
 endif()
