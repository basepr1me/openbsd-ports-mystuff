$OpenBSD$

Index: gerbview/CMakeLists.txt
--- gerbview/CMakeLists.txt.orig
+++ gerbview/CMakeLists.txt
@@ -114,6 +114,8 @@ set_source_files_properties( ${CMAKE_SOURCE_DIR}/commo
 target_link_libraries( gerbview
     #singletop  # replaces common, giving us restrictive control and link warnings.
     # There's way too much crap coming in from common yet.
+    stdc++
+    c
     gal
     common
     nlohmann_json
@@ -156,6 +158,8 @@ set_target_properties( gerbview_kiface PROPERTIES
 target_link_libraries( gerbview_kiface
     gal
     common
+    stdc++
+    c
     ${wxWidgets_LIBRARIES}
     ${GDI_PLUS_LIBRARIES}
     )
