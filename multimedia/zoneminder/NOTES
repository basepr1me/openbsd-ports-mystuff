chown www:www on scripts?

ensure php extensions are activated

How did we change location of mysql db file system wide?
	-env MYSQL_UNIX_PORT

Post Install:

configure mysql socket for /var/www/run/mysql/mysql.sock
set mysqli.default_socket and pdo_mysql.default_socket
mysql -uroot -p < /usr/local/share/zoneminder/db/zm_create.sql

